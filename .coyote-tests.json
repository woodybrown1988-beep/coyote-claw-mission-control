{
  "functionsUnderTest": [
    {
      "module": "mission-control/server.js",
      "export": "formatCount"
    },
    {
      "module": "mission-control/server.js",
      "export": "formatJobAge"
    },
    {
      "module": "mission-control/server.js",
      "export": "formatUtc"
    },
    {
      "module": "mission-control/server.js",
      "export": "statusPillClass"
    },
    {
      "module": "mission-control/server.js",
      "export": "eventTone"
    },
    {
      "module": "mission-control/server.js",
      "export": "isRefusedEvent"
    },
    {
      "module": "mission-control/server.js",
      "export": "summarizeDetail"
    },
    {
      "module": "mission-control/server.js",
      "export": "summarizeTestRun"
    },
    {
      "module": "mission-control/server.js",
      "export": "summarizeLeadDecision"
    },
    {
      "module": "mission-control/server.js",
      "export": "stageProgressPercent"
    },
    {
      "module": "mission-control/server.js",
      "export": "deriveEngine"
    },
    {
      "module": "mission-control/server.js",
      "export": "deriveRef"
    },
    {
      "module": "mission-control/server.js",
      "export": "deriveStage"
    },
    {
      "module": "mission-control/server.js",
      "export": "buildWorkerModel"
    },
    {
      "module": "mission-control/server.js",
      "export": "classifyGateEvent"
    },
    {
      "module": "mission-control/server.js",
      "export": "getKpiSection"
    },
    {
      "module": "mission-control/server.js",
      "export": "countTestIntegrity"
    },
    {
      "module": "mission-control/server.js",
      "export": "getWorkerSection"
    },
    {
      "module": "mission-control/server.js",
      "export": "renderWorker"
    },
    {
      "module": "mission-control/server.js",
      "export": "buildDashboardModel"
    },
    {
      "module": "mission-control/server.js",
      "export": "buildHaltModel"
    },
    {
      "module": "mission-control/server.js",
      "export": "buildDeployModel"
    },
    {
      "module": "mission-control/server.js",
      "export": "deployStatusPillClass"
    },
    {
      "module": "mission-control/ui/pages/claw/agents.js",
      "export": "getSection"
    },
    {
      "module": "mission-control/ui/pages/claw/agents.js",
      "export": "render"
    },
    {
      "module": "mission-control/ui/pages/claw/health.js",
      "export": "getSection"
    },
    {
      "module": "mission-control/ui/pages/claw/health.js",
      "export": "render"
    }
  ],
  "tests": [
    {
      "name": "statusPillClass",
      "command": "node --test test/status-pill-class.test.js"
    },
    {
      "name": "mission-control-event-tone-helper",
      "command": "node --test test/mission-control-events.test.js"
    },
    {
      "name": "mission-control-event-tone-ok",
      "command": "node --test test/mission-control-event-tone-ok.test.js"
    },
    {
      "name": "mission-control-refusal-helper",
      "command": "node --test test/mission-control-events.test.js"
    },
    {
      "name": "mission-control-summary-helper",
      "command": "node --test test/mission-control-events.test.js"
    },
    {
      "name": "mission-control-test-run-summary-integrity",
      "command": "node --test test/summarize-test-run.test.js"
    },
    {
      "name": "mission-control-stage-progress-active-job",
      "command": "node --test test/mission-control-stage-progress-active-job.test.js"
    },
    {
      "name": "mission-control-worker-heartbeat-model",
      "command": "node --test test/build-worker-model.test.js"
    },
    {
      "name": "mission-control-derive-engine",
      "command": "node --test test/derive-engine.test.js"
    },
    {
      "name": "mission-control-kpi",
      "command": "node --test test/mission-control-kpi.test.js"
    },
    {
      "name": "mission-control-worker-monitoring",
      "command": "node --test test/mission-control-worker.test.js"
    },
    {
      "name": "mission-control-halt-dashboard",
      "command": "node --test test/mission-control-halt.test.js"
    },
    {
      "name": "mission-control-deploy-status",
      "command": "node --test test/mission-control-deploy-status.test.js"
    },
    {
      "name": "mission-control-claw-queue-depth",
      "command": "node --test test/mission-control-claw-queue-depth.test.js"
    }
  ],
  "mutants": [
    {
      "name": "formatUtc-old-whole-second-only",
      "module": "mission-control/server.js",
      "find": ".replace(/\\.\\d{3}Z$/, ' UTC')",
      "replace": ".replace('.000Z', ' UTC')"
    },
    {
      "name": "statusPillClass-escalated-not-refused",
      "module": "mission-control/server.js",
      "find": "|(?:^|\\s)escalated(?:\\s|$)",
      "replace": ""
    },
    {
      "name": "summarizeTestRun-old-bare-verdict-pass-count",
      "module": "mission-control/server.js",
      "find": "  if (verdict === 'theatre') {\n    parts.push('integrity theatre/proves-nothing');\n  } else if (verdict === 'accept') {\n    const caughtCount = countCaughtByMutant(detail && detail.perFunction);\n    if (caughtCount > 0) {\n      parts.push(`mutant-killed (${caughtCount} fns)`);\n    }\n  }\n\n",
      "replace": ""
    },
    {
      "name": "stageProgressPercent-awaiting-signoff-zero",
      "module": "mission-control/server.js",
      "find": "  if (normalized === 'awaiting-signoff' || normalized === 'lead-review' || normalized === 'pr' || normalized === 'review') {\n    return 90;\n  }\n",
      "replace": "  if (normalized === 'awaiting-signoff' || normalized === 'lead-review' || normalized === 'pr' || normalized === 'review') {\n    return 0;\n  }\n"
    },
    {
      "name": "eventTone-security-kind-not-bad",
      "module": "mission-control/server.js",
      "find": "  if (normalizeSignal(row.kind) === 'security') {\n    return 'bad';\n  }\n",
      "replace": ""
    },
    {
      "name": "isRefusedEvent-security-kind-refused",
      "module": "mission-control/server.js",
      "find": "function isRefusedEvent(row) {\n  const acceptedTone = acceptedEventTone(row);\n",
      "replace": "function isRefusedEvent(row) {\n  if (normalizeSignal(row && row.kind) === 'security') {\n    return true;\n  }\n  const acceptedTone = acceptedEventTone(row);\n"
    },
    {
      "name": "deriveRef-result-source-removal",
      "module": "mission-control/server.js",
      "find": "function deriveRef(row) {\n  const value = firstPresent(row, [\n    'branch',\n    'ref',\n    'pr',\n    'pr_number',\n    'pull_request',\n    'pull_request_url',\n    'sha',\n    'commit_sha',\n    'head_sha'\n  ]);\n  if (value) {\n    return safeLabel(value, '—');\n  }\n\n  const result = parseDetailObject(row && row.result);\n  for (const key of ['prNumber', 'pr_number', 'number']) {\n    const prNumber = result && result[key];\n    if ((typeof prNumber === 'number' && Number.isFinite(prNumber)) || (typeof prNumber === 'string' && prNumber.trim())) {\n      return `#${String(prNumber).trim()}`;\n    }\n  }\n\n  for (const key of ['branch', 'ref']) {\n    const ref = result && result[key];\n    if (typeof ref === 'string' && ref.trim()) {\n      return safeLabel(ref, '—');\n    }\n  }\n\n  for (const key of ['headSha', 'sha', 'commit_sha']) {\n    const sha = result && result[key];\n    if (typeof sha === 'string' && sha.trim()) {\n      return shortId(sha) || '—';\n    }\n  }\n\n  return '—';\n}",
      "replace": "function deriveRef(row) {\n  const value = firstPresent(row, [\n    'branch',\n    'ref',\n    'pr',\n    'pr_number',\n    'pull_request',\n    'pull_request_url',\n    'sha',\n    'commit_sha',\n    'head_sha'\n  ]);\n  if (value) {\n    return safeLabel(value, '—');\n  }\n\n  return '—';\n}"
    },
    {
      "name": "deriveRef-result-sha-before-pr-number",
      "module": "mission-control/server.js",
      "find": "function deriveRef(row) {\n  const value = firstPresent(row, [\n    'branch',\n    'ref',\n    'pr',\n    'pr_number',\n    'pull_request',\n    'pull_request_url',\n    'sha',\n    'commit_sha',\n    'head_sha'\n  ]);\n  if (value) {\n    return safeLabel(value, '—');\n  }\n\n  const result = parseDetailObject(row && row.result);\n  for (const key of ['prNumber', 'pr_number', 'number']) {\n    const prNumber = result && result[key];\n    if ((typeof prNumber === 'number' && Number.isFinite(prNumber)) || (typeof prNumber === 'string' && prNumber.trim())) {\n      return `#${String(prNumber).trim()}`;\n    }\n  }\n\n  for (const key of ['branch', 'ref']) {\n    const ref = result && result[key];\n    if (typeof ref === 'string' && ref.trim()) {\n      return safeLabel(ref, '—');\n    }\n  }\n\n  for (const key of ['headSha', 'sha', 'commit_sha']) {\n    const sha = result && result[key];\n    if (typeof sha === 'string' && sha.trim()) {\n      return shortId(sha) || '—';\n    }\n  }\n\n  return '—';\n}",
      "replace": "function deriveRef(row) {\n  const value = firstPresent(row, [\n    'branch',\n    'ref',\n    'pr',\n    'pr_number',\n    'pull_request',\n    'pull_request_url',\n    'sha',\n    'commit_sha',\n    'head_sha'\n  ]);\n  if (value) {\n    return safeLabel(value, '—');\n  }\n\n  const result = parseDetailObject(row && row.result);\n  for (const key of ['headSha', 'sha', 'commit_sha']) {\n    const sha = result && result[key];\n    if (typeof sha === 'string' && sha.trim()) {\n      return shortId(sha) || '—';\n    }\n  }\n\n  for (const key of ['prNumber', 'pr_number', 'number']) {\n    const prNumber = result && result[key];\n    if ((typeof prNumber === 'number' && Number.isFinite(prNumber)) || (typeof prNumber === 'string' && prNumber.trim())) {\n      return `#${String(prNumber).trim()}`;\n    }\n  }\n\n  for (const key of ['branch', 'ref']) {\n    const ref = result && result[key];\n    if (typeof ref === 'string' && ref.trim()) {\n      return safeLabel(ref, '—');\n    }\n  }\n\n  return '—';\n}"
    },
    {
      "name": "deploy-up-to-date-flipped-full-hash-comparison",
      "module": "mission-control/server.js",
      "find": "      && servingHash === latestTargetHash\n",
      "replace": "      && servingHash !== latestTargetHash\n"
    },
    {
      "name": "deploy-up-to-date-sha8-only-comparison",
      "module": "mission-control/server.js",
      "find": "      && servingHash === latestTargetHash\n",
      "replace": "      && shortId(servingHash) === shortId(latestTargetHash)\n"
    },
    {
      "name": "deploy-get-commit-linkage-severed",
      "module": "mission-control/server.js",
      "find": "    ...buildDeployModel(result.rows, getCommit()),\n",
      "replace": "    ...buildDeployModel(result.rows, 'unknown'),\n"
    },
    {
      "name": "deploy-broken-to-deployed-normalization",
      "module": "mission-control/server.js",
      "find": "  if (normalized === 'broken') {\n    return 'broken';\n  }\n",
      "replace": "  if (normalized === 'broken') {\n    return 'deployed';\n  }\n"
    },
    {
      "name": "deploy-rolled-back-to-deployed-normalization",
      "module": "mission-control/server.js",
      "find": "  if (normalized === 'rolled_back' || normalized === 'rollback' || normalized === 'roll_back') {\n    return 'rolled_back';\n  }\n",
      "replace": "  if (normalized === 'rolled_back' || normalized === 'rollback' || normalized === 'roll_back') {\n    return 'deployed';\n  }\n"
    },
    {
      "name": "deploy-pending-detection-dropped",
      "module": "mission-control/server.js",
      "find": "    pendingRows: normalizedRows.filter((row) => row.status === 'pending' || row.status === 'deploying'),\n",
      "replace": "    pendingRows: normalizedRows.filter(() => false),\n"
    }
  ]
}
